# Provider readiness — 2026-08-13 IST

This is the current machine/account truth. `READY IN CODE` is not a claim that an external resource exists.

| Provider | Current state | Required live unblock |
|---|---|---|
| Hermes | Local Hermes `v0.18.2` and the repository skill remain verified. The production host path now includes an authenticated loopback origin, commit-pinned runtime installer, restricted systemd units, and a pinned named-tunnel connector. Queue mode, concise busy handling, repository `terminal.cwd`, and an SME inference/one-follow-up platform hint are configured. Sessions remain per WA-ID and the Worker derives tenant identity. AWS infrastructure is live, but the host runtime installation is still in progress. | Finish the runtime install, configure operator-owned host secrets, then prove a second real merchant turn through the named origin |
| Meta WhatsApp | The extended ProofGate token remains valid through `2026-10-07T16:53:08Z`; values are not recorded. The test sender is registered, outbound template delivery is evidenced, and the `messages` subscription is active. Account confirmation is now cleared and the app list is accessible, but ProofGate remains `In development`. Production Setup currently stalls on Meta-side `Failed to fetch`. | Retry Production Setup when Meta loads, connect the production business number, complete required business/review steps, and test a second merchant before general availability |
| Cloudflare | Worker `proofgate-whatsapp-growth` version `71974026-a4dc-4854-ae13-504376929df0` serves the Axcas small-business journey with production Convex/KV. Fresh root/title/brand/CSP checks passed. Internal Worker and callback names remain stable to avoid breaking Meta. The signed-in dashboard is staged at the connector step for `proofgate-hermes-production`, but no connector is active. R2 remains optional/inactive. | After the AWS runtime install completes, connect the staged named tunnel to the authenticated origin on `127.0.0.1:8080`, verify it healthy, then rotate the Worker origin. No R2 action is required. |
| Convex | Development `earnest-mandrill-823` and production `tame-corgi-404` are deployed with typechecking and separate service secrets. The 16 MiB no-card storage fallback verifies magic bytes plus metadata hash/size/type and rejects merchant/backend collisions. A synthetic PNG inserted once and replayed idempotently through the live Worker; its public route correctly returned 404 because no promoted production spec selects it. | Keep raw storage URLs private, clean any unregistered test orphans, and repeat with real merchant assets only during consented acceptance |
| AWS | Root CloudShell access in `ap-south-1` is verified for account `917394547881`. After one pre-resource validation failure (`IamInstanceProfile` shape) and one fully rolled-back attempt (`SecurityGroupEgress` without `VpcId`, with no EC2 survivor), corrected stack `proofgate-foundation` reached `CREATE_COMPLETE`. It created instance `i-06057ce3046b446de` and private recordings bucket `proofgate-foundation-recordingsbucket-k7meu4p2bug6`; the template provides encrypted 24 GB EBS, outbound-only networking, SSM (instance `Online`), Polly IAM, and private encrypted recordings storage. Runtime installation remains in progress and Hermes is not yet claimed live. | Finish the commit-pinned runtime install, configure operator-owned secrets without recording them, connect the named tunnel, and prove the authenticated Hermes origin before changing the Worker origin |
| Vapi | Active free US number `+17609748059` (`3a84a4af-53a6-4baa-9eed-ace2f0a73731`) is assigned. Consent assistant `e7210ebf-91f8-48eb-8cf9-6587b91fe88e` has recording/logging/transcript disabled; qualification assistant `f48272d7-fc58-4433-9b0a-8b51ecdbda10` has all three enabled. Squad `492a8fae-b1ec-4d60-b221-1d7115a53eef` contains exactly those two members with consent-first handoff. The API key was verified and remains outside git. No call has been placed. | Configure and verify the authenticated HMAC callback, then run only the two approved self/test consent-declined and consent-granted acceptance calls |
| FFmpeg | Installed locally; renderer and ffprobe verification built | Verify on AWS host with three merchant assets and one approved plan |
| Instagram | The typed Graph API adapter, exact three-variant campaign hash, one-approval boundary, and reach-normalized 2h/24h/72h scorer are deployed in code. No account, token, post, or insight receipt is claimed. | Connect an Instagram Professional account, obtain least-privilege content-publish/insights permissions, then run a private test campaign only after exact approval |

The two Convex deployments, Cloudflare KV namespace, named Worker/version, and completed
AWS `proofgate-foundation` stack above are provider resources. Public foundation HTTPS,
the Meta app/WABA/test number/callback, the delivered WhatsApp test flow, and the
temporary tunnel are real. No R2 bucket, connected durable named tunnel, live AWS-hosted
Hermes runtime, or Vapi call is claimed yet. No real
non-test call is authorized merely by filling credentials.

## Foundation gate

Convex development/production, the no-R2 storage path, Worker HTTPS, Meta subscription APIs, local Hermes, the append-only merchant decision boundary, the quick-tunnel synthetic forwarding path, and the encrypted AWS infrastructure clear their respective foundation gates. Convex File Storage is the current private immutable asset backend; it is not an R2 claim. The AWS host runtime installation is still in progress and is not live-Hermes evidence. The decision API is deployed and fails closed when no merchant policy exists; the first real policy record awaits complete merchant intake. Live acceptance still requires the real signed Meta delivery to reach Hermes, a named Hermes origin, real merchant intake/publish approval, and the documented Vapi self-tests. The quick tunnel cannot onboard an external merchant.

Current Worker origin configuration is the remote KV key `hermes_origin`, pointing to the health-checked foundation tunnel `https://sara-version-try-vic.trycloudflare.com`. The 2026-08-09 decision-engine deployment removed the earlier API-added plain-text origin variable, while preserving all encrypted secrets; KV was set and read back immediately afterward. This is a recoverable configuration fallback, not a named production origin.

The Meta app callback and WABA link were active but the app subscription initially had no webhook fields. The `messages` field is now subscribed at Graph API v26.0 and confirmed by a read-back. A signed duplicate fixture traversed Worker → KV origin → quick tunnel → Hermes and incremented Hermes' duplicate counter, proving the internal route. Live acceptance remains open until a new allowlisted merchant message increments `accepted` and produces the corresponding agent receipt.

That live ingress gate is now passed: the allowlisted merchant sent `START`, Hermes incremented `accepted` to 1 with no signature rejection, completed an agent turn, and the merchant received the Cloud API reply. The reply was generic, so the ProofGate skill and WhatsApp Cloud platform hint were tightened and the chat was persisted as the home channel. The installed gateway now starts at Windows login. Merchant bakery intake remains the next gate; the separate-process CLI cannot proactively send through this adapter because Hermes v0.18.2 does not register a standalone WhatsApp Cloud sender.

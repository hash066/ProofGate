# Provider readiness — 2026-08-13 IST

This is the current machine/account truth. `READY IN CODE` is not a claim that an external resource exists.

Current readiness: **96% controlled-beta ready; 94% external-production ready**.

| Provider | Current state | Required live unblock |
|---|---|---|
| Hermes | AWS runs exact repository commit `9c5835baf4ec55fce6d9ec10bbf8dc1d2cedfa35` with Hermes `v0.18.2`, `aiohttp 3.14.1`, and `httpx 0.28.1`. Gateway, authenticated origin, and quick-tunnel services are active. Port `8090` loopback health reports `whatsapp_cloud`, verify/app-secret configuration, FFmpeg, and zero accepted/duplicate/rejected-signature counters; port `8080` origin health is `ok`. Initial host-permission and missing-messaging-dependency failures were diagnosed and fixed. Earlier Node 18 `EBADENGINE` warnings were superseded by verified Node `v22.23.2` and npm `10.9.8`; the restarted origin remains active and healthy. | Establish a durable named origin, then prove a second real merchant turn through live Meta delivery |
| Meta WhatsApp | The extended ProofGate token remains valid through `2026-10-07T16:53:08Z`; values are not recorded. The test sender is registered, outbound template delivery is evidenced, and the `messages` subscription is active. Account confirmation is now cleared and the app list is accessible, but ProofGate remains `In development`. Production Setup currently stalls on Meta-side `Failed to fetch`. | Retry Production Setup when Meta loads, connect the production business number, complete required business/review steps, and test a second merchant before general availability |
| Cloudflare | Worker `proofgate-whatsapp-growth` version `71974026-a4dc-4854-ae13-504376929df0` serves the Axcas small-business journey with production Convex/KV. AWS quick URL `https://propose-mainly-operator-disabled.trycloudflare.com` returned origin health `ok`, and unauthenticated webhook POST returned 401. `PROOFGATE_CONFIG` key `hermes_origin` was updated to that exact URL and dashboard readback matched. A signed empty synthetic envelope traversed the public Worker with HTTP 200, but created no accepted message. The account still has no domain/subdomain; staged named tunnel has zero routes and is inactive. | Add a custom domain/named route and replace the temporary quick URL. Then prove a real second-merchant Meta intake. No R2 action is required. |
| Convex | Development `earnest-mandrill-823` and production `tame-corgi-404` are deployed with typechecking and separate service secrets. The 16 MiB no-card storage fallback verifies magic bytes plus metadata hash/size/type and rejects merchant/backend collisions. A synthetic PNG inserted once and replayed idempotently through the live Worker; its public route correctly returned 404 because no promoted production spec selects it. | Keep raw storage URLs private, clean any unregistered test orphans, and repeat with real merchant assets only during consented acceptance |
| AWS | Root CloudShell access in `ap-south-1` is verified for account `917394547881`; corrected stack `proofgate-foundation` is `CREATE_COMPLETE` with instance `i-06057ce3046b446de`, private recordings bucket `proofgate-foundation-recordingsbucket-k7meu4p2bug6`, encrypted 24 GB EBS, outbound-only networking, SSM `Online`, and Polly IAM. Exact commit `9c5835baf4ec55fce6d9ec10bbf8dc1d2cedfa35` is installed; gateway, origin, and quick-tunnel services are active. Official Node `v22.23.2` and npm `10.9.8` are installed from the `nodejs.org` tarball after a successful SHA-256 check; repository CloudFormation pins that same tarball/checksum. Three known S3 bootstrap object versions were permanently deleted; prefix `bootstrap/` has no versions/delete markers. Five exact CloudShell paths were checked absent: `.hermes-upload.env`, `.env`, `hermes-prod.env`, `origin-prod.env`, and `tunnel-token` under `/home/cloudshell-user`. | Replace temporary quick-tunnel reachability with a durable named route |
| Vapi | Active free US number `+17609748059` (`3a84a4af-53a6-4baa-9eed-ace2f0a73731`) is assigned. Consent assistant `e7210ebf-91f8-48eb-8cf9-6587b91fe88e` has recording/logging/transcript disabled; qualification assistant `f48272d7-fc58-4433-9b0a-8b51ecdbda10` has all three enabled. Squad `492a8fae-b1ec-4d60-b221-1d7115a53eef` contains exactly those two members with consent-first handoff. The API key was verified and remains outside git. No call has been placed. | Configure and verify the authenticated HMAC callback, then run only the two approved self/test consent-declined and consent-granted acceptance calls |
| FFmpeg | Installed locally and reported available by AWS Hermes health; renderer and ffprobe verification are built | Verify an AWS render with three merchant assets and one approved plan |
| Instagram | The typed Graph API adapter, exact three-variant campaign hash, one-approval boundary, and reach-normalized 2h/24h/72h scorer are deployed in code. No account, token, post, or insight receipt is claimed. | Connect an Instagram Professional account, obtain least-privilege content-publish/insights permissions, then run a private test campaign only after exact approval |

The two Convex deployments, Cloudflare KV namespace, named Worker/version, and completed
AWS `proofgate-foundation` stack above are provider resources. Public foundation HTTPS,
the Meta app/WABA/test number/callback, the delivered WhatsApp test flow, and the
temporary tunnels are real. The AWS-hosted Hermes gateway and origin are healthy, and
the AWS quick URL is externally reachable with its unauthenticated webhook failing
closed. No R2 bucket, connected durable named tunnel, live Meta delivery through AWS,
received WhatsApp message through AWS, or Vapi call is claimed yet. No real
non-test call is authorized merely by filling credentials.

## Foundation gate

Convex development/production, the no-R2 storage path, Worker HTTPS, Meta subscription APIs, local Hermes, the append-only merchant decision boundary, encrypted AWS infrastructure, AWS Hermes/origin runtime, temporary AWS external reachability, and the synthetic Worker-to-AWS transport check clear their respective foundation gates. Convex File Storage is the current private immutable asset backend; it is not an R2 claim. A durable public origin is the remaining infrastructure blocker for external onboarding: the Cloudflare account has no routeable domain/subdomain and the staged tunnel has zero routes. The decision API is deployed and fails closed when no merchant policy exists; the first real policy record awaits complete merchant intake. Live acceptance still requires a named route, real signed Meta delivery with a message entry, second-merchant intake/publish approval, and the documented Vapi self-tests. The quick tunnel is foundation-only and cannot establish general availability.

Current Worker origin configuration is remote `PROOFGATE_CONFIG` KV key `hermes_origin`, pointing to the health-checked AWS foundation tunnel `https://propose-mainly-operator-disabled.trycloudflare.com`; the Cloudflare dashboard readback matched exactly. This supersedes the prior laptop quick URL. It is a recoverable temporary configuration fallback, not a named production origin.

The Meta app callback and WABA link were active but the app subscription initially had no webhook fields. The `messages` field is now subscribed at Graph API v26.0 and confirmed by a read-back. A signed duplicate fixture traversed Worker → KV origin → quick tunnel → Hermes and incremented Hermes' duplicate counter, proving the internal route. Live acceptance remains open until a new allowlisted merchant message increments `accepted` and produces the corresponding agent receipt.

That live ingress gate is now passed: the allowlisted merchant sent `START`, Hermes incremented `accepted` to 1 with no signature rejection, completed an agent turn, and the merchant received the Cloud API reply. The reply was generic, so the ProofGate skill and WhatsApp Cloud platform hint were tightened and the chat was persisted as the home channel. The installed gateway now starts at Windows login. Merchant bakery intake remains the next gate; the separate-process CLI cannot proactively send through this adapter because Hermes v0.18.2 does not register a standalone WhatsApp Cloud sender.

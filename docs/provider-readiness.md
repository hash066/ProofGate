# Provider readiness — 2026-08-19 IST

This is the current machine/account truth. `READY IN CODE` is not a claim that live acceptance has happened.

Original WhatsApp MVP readiness remains **96% technically complete; about 86% smooth-beta ready**. Against the expanded Product Hunt scope (WhatsApp + Axcas Studio + web projects + reference-reel layers), readiness is about **88%**. Studio website projects now run from structured facts and private uploads through deterministic business-type inference, an immutable candidate, a separate no-credential verifier, one owner checklist, and deterministic publication. A real merchant-media reel render/delivery, public account-link abuse protection, and a production Meta number remain open.

| Provider | Current state | Required live unblock |
|---|---|---|
| Hermes | AWS runs repository commit `c7434b689913eb991f783c1ca349c64b779d3c29` with Hermes `v0.18.2`, Node `v22.23.2`, FFmpeg, Remotion Headless Shell, the official Cloud adapter, and active gateway/origin/relay services. Secrets Manager supplies the Worker admin credential, both admin variables are configured, and loopback health passes. The stable relay also returned external HTTP 200 after deployment. | Register the production number, then prove a bounded live merchant turn using a real merchant profile |
| Meta WhatsApp | App `2349611939193122` is **Published** under the Axcas display name. Privacy, terms, deletion, callback, WABA link, and `messages` subscription are saved. The Meta test number remains configured, but arbitrary recipients are rejected with Graph error `131030` because no real production number is registered. | Register the real business number and complete its OTP. Add billing only when Meta requires it for production messaging. Business verification requires genuine business documents and cannot be simulated. |
| Cloudflare | Main Worker version `ef8d847b-33e0-4000-bf05-1b6f24d05a32` serves the Product Hunt home, checked Studio website build/publish flow, legal pages, signed Meta/Vapi webhooks, production Convex/KV, and private signed previews. Separate verifier Worker version `06f67c39-4926-46e9-ba58-6eecac8bc66d` has no credentials or mutation authority and can spend only the supplied single-use evidence capability. Fresh main/verifier health and Studio checks returned HTTP 200; an unauthenticated Studio project request returned 401. KV `hermes_origin` remains the stable AWS API Gateway. | Add public account-link abuse protection and prove a real linked merchant session |
| Convex | Development `earnest-mandrill-823` and production `tame-corgi-404` are deployed with separate service secrets. Production now includes indexed short-lived Studio links, hashed sessions, and append-only project revisions. The 16 MiB no-card storage package verifies magic bytes plus metadata hash/size/type, rejects merchant/backend collisions, and keeps candidate/reference assets private. Customers create no storage account and supply no key. | Complete one WhatsApp-linked session, private reference upload, and web-project candidate |
| Axcas Studio | Live `/studio` offers Website, Reels, or Both. The website path collects plain-language business facts and real media, stores an append-only revision, infers the constrained SME type, builds only validated data, exposes a private preview, runs an isolated verifier, and asks for one final publication checklist. The linked-session approval is owner-hash and merchant bound. No real linked merchant publication is claimed yet. | Complete one real Meta-signed browser link and web-to-publish acceptance; finish automatic approved reel rendering/delivery |
| AWS | Stack `proofgate-foundation` is `UPDATE_COMPLETE` in account `917394547881`, `ap-south-1`. It includes encrypted EBS, private encrypted 30-day S3 recordings, Polly IAM, SSM, encrypted SQS/DLQ, generated relay secret, Lambda ingress, stable HTTP API, and a separate generated Hermes admin secret with least-privilege EC2 read access. Instance `i-06057ce3046b446de` remains outbound-only. Stable health returns 200; unauthenticated webhook POST returns 401. | Monitor the relay/DLQ during the real production-number merchant run |
| Vapi | Active US number `+17609748059` is assigned. The two-member squad is renamed Axcas Consent-First Qualification. Consent artifacts remain off; qualification recording/logging/transcript remain on. Both assistants send only `end-of-call-report` to the Worker using HMAC credential `cfbe1546-c361-4951-a2bf-1ef665dba3ec`. A locally signed synthetic callback was authenticated by production and returned 200 without creating a call outcome. No call has been placed. | Run only the separately approved self/test consent-declined and consent-granted calls |
| FFmpeg / Remotion | The owner-authorized template source is pinned at `ed8d037f7b35e0cc971521801df07e0edf69828c`. Five compositions map to typed Studio formats, accept only selected assets under an approved root, and exclude creator-specific samples. AWS exact commit `c7434b689913eb991f783c1ca349c64b779d3c29` rendered a synthetic 15.000-second 1,655,633-byte smoke reel; ffprobe returned 1080×1920 H.264 video and AAC audio. The ephemeral request and MP4 were deleted after the receipt. | Verify the same path with merchant-supplied assets and one approved plan, then deliver it privately |
| Instagram | The typed Graph API adapter, exact three-variant campaign hash, one-approval boundary, and reach-normalized 2h/24h/72h scorer are deployed in code. No account, token, post, or insight receipt is claimed. | Connect an Instagram Professional account with OAuth and approve one exact campaign before any publishing |

## Foundation gate

Convex development/production, the no-R2 storage path, Worker HTTPS, published Meta app, append-only merchant decision boundary, encrypted AWS infrastructure, stable AWS Hermes relay, and authenticated Vapi callback clear their foundation gates. Convex File Storage is the current private immutable asset backend; it is not an R2 claim. The decision API is deployed and fails closed when no merchant policy exists.

Current Worker origin configuration is remote `PROOFGATE_CONFIG` KV key `hermes_origin`, pointing to stable API Gateway `https://6glzwgtc2g.execute-api.ap-south-1.amazonaws.com`; remote Wrangler readback matched exactly. Expired quick URLs are historical only.

The Meta app is published and `messages` is subscribed at Graph API v26.0. Live acceptance remains open until a post-repair merchant message traverses the stable AWS relay and produces the corresponding agent receipt. General users require one operator-owned production number and its one-time OTP registration; merchants then only message that shared number. The Meta test number cannot reply to arbitrary recipients and returned Graph error `131030` again during the Golden Crust recovery attempt.

No R2 bucket, production-number OTP, live Meta message through the new durable relay, second-merchant production run, real merchant reel, or Vapi call is claimed. No real call is authorized merely by filling credentials.

# Provider evidence — how each power-up is EARNED (not claimed)

Authoritative spec: PROOFGATE_BUILD_BIBLE.md §17 (pre-event readiness + env), §24 (power-up earn rules), §4 (voice evidence ladder). This is the condensed checklist; fill the pointers in EVIDENCE.md as each lands. "Activated account" earns nothing; a mentor must see it working.

| Partner | Wire it | Earn moment | Pre-event admin (DO TODAY) |
|---|---|---|---|
| Cloudflare | Pages deploys via API/wrangler; passport hosted | Mentor opens live URL + CF dashboard | Account + API token scoped to Pages |
| Convex | All state (schema in repo) | Dashboard shows real rows during demo | `npx convex dev` once; note deployment URL |
| ElevenLabs | Fresh TTS voice confirmation that is a required contract clause and changes product state (§24: Hermes' own Whisper/OpenAI voice-note intake does NOT count; Scribe STT only when deliberately configured and evidenced) | Mentor hears the fresh TTS note arrive; request ID stored | Redeem Creator coupon: Discord → #coupon-codes → GrowthX Hackathon form; generate one private health-check clip |
| Linkup | Evidence specialist verifies `claims[]` before render | One live query shown in code + log | Sign up; Settings → Add Credits → code **HERMES** ($50) |
| Wispr Flow | Dictate PR descriptions / EVIDENCE entries | Stats screenshot ≥500 words — set hour-2 and hour-5 reminders | Sign up via the perk link (3 months) |
| Dodo | Checkout on `payment` contracts + webhook → Convex | Live-mode payment row + webhook match | **Start KYC NOW (days of lead time)**; apply zero-fees code from perk email; status as track-3 power-up is disputed — ask organizer on the day |
| OpenAI | ($200 credits + Codex Pro month) available for build-time codegen | n/a (not a power-up) | Only granted if org ID was given at registration |
